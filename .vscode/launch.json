{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/sandbox/target/debug/hello-rust",
            "args": [],
            "cwd": "${workspaceRoot}/sandbox",
        }
    ]
}