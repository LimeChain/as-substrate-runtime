{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/sandbox/target/debug/sandbox-execution-environment",
            "args": [],
            "cwd": "${workspaceRoot}/sandbox",
        }
    ]
}